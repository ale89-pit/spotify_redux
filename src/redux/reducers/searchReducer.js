const initialState = {
  content: [],
};

const searchResult = (state = initialState, action) => {
  switch (action.type) {
    case SEARCH_NAME:
    default:
      return state;
  }
};

export default searchResult;
